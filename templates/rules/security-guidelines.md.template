---
_harness_template: "rules/security-guidelines.md.template"
_harness_version: "2.5.30"
description: "Security guidelines"
---

# Security Guidelines

> This rule was auto-generated by Claude harness.
> Purpose: detect changes that warrant security attention and present a checklist.

## Patterns That Warrant Security Attention

### File Path Assessment

| Path Pattern | Concern Level | Reason |
|-------------|-----------|------|
| `src/auth/`, `auth/` | 🔴 High | Authentication logic |
| `src/api/`, `api/`, `routes/` | 🟠 Medium | API endpoints |
| `middleware/` | 🟠 Medium | Request processing |
| `src/payment/`, `billing/` | 🔴 High | Payment processing |
| `config/`, `.env*` | 🟠 Medium | Sensitive configuration |

### Code Change Patterns

| Pattern | Concern Level | Reason |
|---------|-----------|------|
| `password`, `secret`, `token` | 🔴 High | Credentials |
| Dynamic code evaluation functions | 🔴 High | Arbitrary code execution risk |
| String concatenation in SQL queries | 🔴 High | SQL injection |
| Raw HTML insertion in React | 🟠 Medium | XSS risk |
| `cors()` configuration change | 🟠 Medium | Cross-origin |

## OWASP Top 10 Checklist

### When Changing Authentication/Authorization

- [ ] **A01: Broken Access Control**
  - Do authorization checks exist on every endpoint?
  - Is role-based access control correct?

- [ ] **A02: Cryptographic Failures**
  - Are passwords hashed (bcrypt, argon2)?
  - Is sensitive data encrypted?
  - Is HTTPS enforced?

- [ ] **A07: Authentication Failures**
  - Brute-force protection (rate limiting)
  - Is session management appropriate?
  - Have you considered MFA support?

### When Changing APIs

- [ ] **A03: Injection**
  - Is input sanitized?
  - Are parameterized queries used?
  - Command injection protection

- [ ] **A04: Insecure Design**
  - Principle of least privilege
  - Is input validation implemented at the business-logic level?

- [ ] **A05: Security Misconfiguration**
  - Are unnecessary endpoints exposed?
  - Are error messages too detailed?
  - Have default credentials been changed?

### When Changing Dependencies

- [ ] **A06: Vulnerable and Outdated Components**
  - Do the packages you add have known vulnerabilities (`npm audit`)?
  - Are the versions current?
  - Are they maintained?

## Assessment Logic for Skills

```
File change detected
    ↓
Path pattern match
    ↓
├── auth/, api/, payment/ → show security checklist
├── middleware/ → CORS, authorization check
├── .env*, config/ → sensitive-info exposure check
└── other → normal processing
```

## Suggestion Templates

**When changing authentication-related code**:
> ⚠️ Detected an authentication-related code change.
> Please review the security checklist:
> - [ ] Password hashing
> - [ ] Session management
> - [ ] Rate limiting

**When changing APIs**:
> ⚠️ Detected an API endpoint change.
> - [ ] Input validation
> - [ ] Authorization check
> - [ ] Error handling

## For VibeCoders (Rephrased)

| Technical Term | For VibeCoders |
|---------|---------------|
| Authentication | Login/password-related |
| Authorization | Controlling who can do what |
| Sanitize | Neutralize dangerous characters |
| Injection | Injecting malicious code |

**Suggestion for VibeCoders**:
> 🔐 This change involves login.
> We recommend having a security expert review it.
> At a minimum, check the following:
> - Are passwords stored in plaintext?
> - Is there a limit on login attempts?

## Notes

- **Warnings are suggestions**: the user makes the final call
- **Tolerate false positives**: err on the safe side
- **Recommend expert review**: important changes should be reviewed by a human
