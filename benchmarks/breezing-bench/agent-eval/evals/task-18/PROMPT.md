Add an `applyDiscount(invoiceId, discountPercent)` method to InvoiceCalculator.
The interface definition is in `types.ts`.
It applies a percentage discount and recalculates the subtotal, tax, and total.
