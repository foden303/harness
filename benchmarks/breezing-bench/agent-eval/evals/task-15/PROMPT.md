Add `validateEmail(email: string)` and `validateUrl(url: string)` methods to FormValidator.
The interface definition is in `types.ts`.
Each returns its validation result as { valid: boolean, error?: string }.
